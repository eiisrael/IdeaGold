{
  "name": "ideagold",
  "version": "3.0.0",
  "private": true,
  "description": "IdeaGold 3.0: mineração XMR em um clique, XMRig verificado, telemetria e Profit Guard.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "worker": "node worker-agent.js",
    "check": "node --check server.js && node --check worker-agent.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ccxt": "^4.5.0"
  }
}
