'use strict';
const fs=require('fs'),path=require('path'),{spawnSync}=require('child_process');const root=path.resolve(__dirname,'..'),dirs=['backend','database','miner','optimizer','providers','frontend','test','lib','tools'];let files=[];for(const d of dirs){const base=path.join(root,d);if(!fs.existsSync(base))continue;for(const name of fs.readdirSync(base)){const f=path.join(base,name);if(fs.statSync(f).isFile()&&f.endsWith('.js'))files.push(f)}}for(const f of files){const r=spawnSync(process.execPath,['--check',f],{stdio:'inherit'});if(r.status!==0)process.exit(r.status||1)}console.log(`syntax check: ${files.length} JS files OK`);
