# Status da remodelação V5

Código da remodelação concluído na branch `agent/ideagold-v5-mining-intelligence`.

O estado de release é controlado pelo workflow `IdeaGold CI` e pelos gates descritos em `VALIDATION_PLAN.md`.

Integrações dependentes de hardware físico (Huge Pages, MSR, temperatura, wattímetro, hashrate/pool real) são intencionalmente verificadas no computador do usuário e nunca simuladas no CI.
