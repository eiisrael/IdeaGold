{
  "name": "ideagold-v2",
  "version": "2.0.0",
  "private": true,
  "description": "GoldMesh: painel de mineração distribuída, telemetria XMRig, rentabilidade e conversão via exchanges.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "worker": "node worker-agent.js",
    "check": "node --check server.js && node --check worker-agent.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ccxt": "^4.5.0"
  }
}
