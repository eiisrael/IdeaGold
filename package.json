{
  "name": "ideagold-v2",
  "version": "2.1.0",
  "private": true,
  "description": "GoldMesh Profit Guard: mineração autorizada, telemetria XMRig, perfil i5-4670K/RX460, energia, rentabilidade e conversão via exchanges.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "worker": "node worker-agent.js",
    "check": "node --check server.js && node --check worker-agent.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ccxt": "^4.5.0"
  }
}
